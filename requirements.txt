# Thin alias for users who prefer the historical install command.
# Real metadata lives in pyproject.toml; this file installs the project in
# editable mode with both optional extras (owlready2 reasoner + dev tools).
#
#     pip install -r requirements.txt
#
# Equivalent to:
#
#     pip install -e ".[dev,reasoner]"
-e .[dev,reasoner]
